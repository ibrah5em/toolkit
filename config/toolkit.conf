# =====================================================
# Toolkit Configuration
# =====================================================
# Override these values to customize paths.
# Environment variables take precedence over this file.
# User config at ~/.config/toolkit/config overrides this file.

# Scripts directory (auto-detected from this file's location if unset)
# SCRIPTS_DIR="$HOME/scripts"

# Projects directory for project-init.sh
PROJECTS_DIR="$HOME/projects"

# Notes directory for notes.sh
NOTES_DIR="$HOME/notes"
